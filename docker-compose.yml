services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  shardstream:
    build: .
    environment:
      - REDIS_ADDR=redis:6379
    ports:
      - "8080:8080"
    depends_on:
      - redis
